{
    "jobs": {
        "main": { "requires": ["~pr", "~commit"] },
        "foo": { "requires": ["main"] },
        "bar": { "requires": ["~foo", "~promote"] },
        "promote": { "requires": [] },
        "baz": { "requires": ["~release", "~tag"] }
    }
}
